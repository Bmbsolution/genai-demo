# Flat node_modules so the Netlify Next.js runtime can trace and bundle the
# `next` package into the serverless function (pnpm's default symlinked layout
# breaks file tracing → "Cannot find module next/dist/server/lib/start-server.js").
node-linker=hoisted
